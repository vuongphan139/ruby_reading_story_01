<% provide :title, current_user.name  %>
<div class="row title">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= link_to gravatar_for(current_user, size: Settings.gravatar_size_user), current_user %>
        <%= current_user.name %>
      </h1>
    </section>
  </aside>
</div>
