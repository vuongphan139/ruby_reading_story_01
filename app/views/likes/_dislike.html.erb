<span><%= pluralize @story.count_like, t(".like") %></span>
<%= form_with url: like_path(@story.id), method: :delete, remote: true do |f| %>
  <%= hidden_field_tag :story_id, @story.id %>
  <%= f.submit t(".dislike"), class: "btn btn-danger ml-3" %>
<% end %>
